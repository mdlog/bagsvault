# Backend API base URL (consumed via process.env.REACT_APP_BACKEND_URL)
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional: enable webpack health endpoints (/health, /health/ready, /health/live, ...)
ENABLE_HEALTH_CHECK=false
